{
  "name": "lambda-html-to-pdf",
  "version": "1.0.0",
  "license" : "UNLICENSED",
  "description": "AWS Lambda function that converts HTML pages to PDF documents",
  "scripts": {
    "lint": "./node_modules/.bin/eslint --quiet .",
    "pack": "rm -f package.zip && zip package.zip -r index.js wkhtmltopdf utils",
    "deploy-dev": "npm run pack && aws lambda update-function-code --region eu-west-1 --function-name htmlToPdf.dev --zip-file fileb://`pwd`/package.zip",
    "deploy-test": "npm run pack && aws lambda update-function-code --region eu-west-1 --function-name htmlToPdf.test --zip-file fileb://`pwd`/package.zip",
    "deploy-prod": "npm run pack && aws lambda update-function-code --region eu-west-1 --function-name htmlToPdf --zip-file fileb://`pwd`/package.zip"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Uninite/lambda-html-to-pdf.git"
  },
  "bugs": {
    "url": "https://github.com/Uninite/lambda-html-to-pdf/issues"
  }
}
